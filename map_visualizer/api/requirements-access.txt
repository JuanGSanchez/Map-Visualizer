# Map-Visualizer access layer — additional dependencies.
# These are declared under [project.optional-dependencies] api in pyproject.toml.
# Install with:  pip install "map-visualizer[api]"
#
# Pinned versions from research-map-visualizer.md PIN TABLE (2026-06-12).
# Note: this layer uses fastmcp directly (not fastapi-mcp); the fastapi-mcp
# entry is intentionally omitted.

fastmcp>=3.4,<4          # 3.4.2 confirmed — FastMCP dual-exposure + Image return
fastapi>=0.136,<1        # 0.136.3 confirmed — REST framework
uvicorn>=0.49,<1         # 0.49.0 confirmed — ASGI server
